{
  "scripts": {
    "build": "npm install --prefix frontend && npx --prefix frontend cross-env NODE_ENV=production npm run build --prefix frontend && mv frontend/.next ./.next",
    "start": "npx --prefix frontend cross-env NODE_ENV=production npm start --prefix frontend"
  }
}